import 'package:flutter/material.dart';
class goauth extends StatefulWidget {
  const goauth({super.key});

  @override
  State<goauth> createState() => _goauthState();
}

class _goauthState extends State<goauth> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [

        ],
      ),
    );
  }
}
